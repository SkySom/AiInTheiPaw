twitch:
  command-prefix: "!"
  enabled: "${twitch:true}"
  client-id: "clientId"
  client-secret: "secret"
  oauth-secret: "secret"
  event-sub-secret: "secret"
  event-callback-domain: "localhost:9000"